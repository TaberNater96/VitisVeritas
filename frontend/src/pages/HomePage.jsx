import Hero from '../components/ui/Hero';

const HomePage = () => {
  return (
    <div className="home-page">
      <Hero />
      {/* Future sections like About, Features, etc. can be added here */}
    </div>
  );
};

export default HomePage;
